<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.crm.dao.DicValueDao">
    <select id="getText" resultType="com.crm.bean.DicValue">
        select * from t_data where dataTypeCode=#{dataTypeCode} order by orderNo
    </select>
</mapper>